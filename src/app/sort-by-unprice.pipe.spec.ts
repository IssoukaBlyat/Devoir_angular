import { SortByUnpricePipe } from './sort-by-unprice.pipe';

describe('SortByUnpricePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByUnpricePipe();
    expect(pipe).toBeTruthy();
  });
});
